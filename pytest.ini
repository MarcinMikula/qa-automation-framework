[pytest]
markers =
    unit: unit-level tests for isolated logic, models, and small rules
    integration: integration/API tests using Service Objects or local services
    e2e: browser-based end-to-end tests using Page Objects
    external: tests requiring live external systems or third-party services; excluded from default CI runs
testpaths =
    tests
addopts =
    -ra
